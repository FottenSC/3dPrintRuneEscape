{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Current File (uv venv)",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "python": "${workspaceFolder}\\.venv\\Scripts\\python.exe"
        }
    ]
}